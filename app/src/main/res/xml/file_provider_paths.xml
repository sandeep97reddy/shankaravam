<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- G5 report sharing: PDFs/CSVs staged under cacheDir/reports. -->
    <cache-path name="reports" path="reports/" />
    <!-- G5 expense receipts live under filesDir/receipts (WebP ~100KB). -->
    <files-path name="receipts" path="receipts/" />
    <!-- Announcement audio clips cached under cacheDir/audio. -->
    <cache-path name="audio" path="audio/" />
    <!-- F2 QR invite images staged under cacheDir/share for WhatsApp/FileProvider share. -->
    <cache-path name="share" path="share/" />
</paths>
