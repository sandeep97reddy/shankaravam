{
  "name": "shankaravam-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "ShankaRavam temple-media gateway: private R2 audio/receipt sharing behind Firebase-seat auth. Phones hold zero secrets.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "wrangler": "^3.99.0"
  }
}
